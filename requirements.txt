# requirements.txt - Root level for Railway deployment
# This file is for Railway.app deployment compatibility

# Core web framework
Flask==2.3.3
flask-cors==4.0.0

# Security and authentication
PyJWT==2.8.0
bcrypt==4.0.1
cryptography==41.0.4
Werkzeug==2.3.7

# Database
# sqlite3 is built into Python, no need to install

# MT5 Integration (Windows only) - Skip on Linux
# MetaTrader5==5.0.45

# Utilities
python-dotenv==1.0.0

# Additional dependencies for production
gunicorn==21.2.0
